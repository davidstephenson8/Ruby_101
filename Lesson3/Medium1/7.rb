=begin

I don't think that this changes anything. The each method returns a new hash, but doesn't modify the input hash.

I was wrong. Lol. The each method is modifying the hashes because thte methods in the block are modifying the hash. Not the each method itself. 

=end